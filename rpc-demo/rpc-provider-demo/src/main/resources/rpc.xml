<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration SYSTEM "http://rpc.com/rpc-config.dtd">
<configuration>

    <port>8092</port>
    <appName>yrpc-default-appName</appName>
    <registry url="zookeeper://127.0.0.1:2181"/>

    <!-- 二选一 -->
    <serializeType type="hessian"/>
    <serializer class="com.DDT.serialize.impl.HessianSerializer"/>

    <!-- 二选一 -->
    <compressType type="gzip"/>
    <compressor class="com.DDT.compress.impl.GzipCompressor"/>

    <!-- 二选一 -->
    <loadBalancer class="com.DDT.loadbalancer.impl.MinimumResponseTimeLoadBalancer"/>
    <loadBalancerType type="minimumResponseTime"/>

    <idGenerator class="com.DDT.IdGenerator" dataCenterId="2" MachineId="4"/>

</configuration>